const tailwindcss = require('tailwindcss');
const autoprefixer = require('autoprefixer');
//const postccsImport = require('postcss-import');
module.exports = {
    plugins: [
        // postccsImport,
        tailwindcss,
        autoprefixer
    ],
};